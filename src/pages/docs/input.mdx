---
layout: 'src/layouts/docs.astro'
---

## Input <small class="clr-subtext txt-sm txt-lowercase"> `<input/>` </small>

`<input/>` elements in web development allow users to enter data into a form.
They come in various types such as text, password, email, and more, each
designed for different types of data entry. Inputs are essential for
gathering user information and enabling interactivity within forms.

<grid class="grid cols-2 g-sm">
  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="button" value="input type button"/>
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="button" value="input type button"/>
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="checkbox" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="checkbox" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="email" placeholder="email@example.com"/>
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="email" placeholder="email@example.com" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="file" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input id="demo-file" type="file" />
      <button>invoke file</button>
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="image" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="image" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="password" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="password" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="radio" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="radio" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="time" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="time" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="range" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="range" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="reset" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="reset" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="url" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="url" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="color" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="color" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="date" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="date" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="datetime-local" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="datetime-local" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="hidden" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="hidden" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="month" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="month" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="number" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="number" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="search" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="search" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="submit" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="submit" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="tel" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="tel" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="text" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="text" />
    </column>
  </grid>

  <grid class='grid outln outln-primary radius-3xs'>
    ```html
    <input type="week" />
    ```
    <column class='align-center[items] flex place-center p-5xl'>
      <input type="week" />
    </column>
  </grid>
</grid>
### -- parentElement <small class="clr-subtext txt-sm"> PROPS </small>

| prop        | description                         | applies to      |
| ----------- | ----------------------------------- | --------------- |
| - - part    | changes style to look like a button | all             |
| - - switch  | sets outline style                  | checkbox, radio |

<grid class="grid cols-2 g-sm">
    <column class="grid outln outln-primary radius-3xs">
        ```html
        <input type="text" data-props="--part" placeholder="very discrete" />
        ```

        <div class="flex place-center p-5xl">
          <input type="text" data-props="--part" placeholder="very discrete"/>
        </div>

    </column>

    <column class="grid outln outln-primary radius-3xs">
        ```html
        <input type="checkbox" data-props="--switch" />
        ```

        <div class="flex place-center p-5xl">
         <input type="checkbox" data-props="--switch" />
        </div>

    </column>

</grid>