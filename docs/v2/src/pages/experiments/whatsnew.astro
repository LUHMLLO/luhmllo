---
import Layout from '../../layouts/test.astro';
---

<Layout>
	<x-grid
		style='gap: var(--nm); padding: var(--nm); height: 100%; overflow: auto; grid-auto-rows: max-content;'>
		<x-wrap style='gap: inherit;'>
			<h2 style='font-size: var(--text-xl); font-weight: bold;'>Development</h2>
			<h1 style='font-size: var(--text-2xl); font-weight: bold;'>What's New</h1>
			<p style='color: var(--text-muted); font-size: var(--text-base);'>
				The latest updates and improvements to MCP
			</p>
		</x-wrap>

		<x-grid style='gap: var(--lg); margin-top: var(--lg);'>
			<!-- Entry -->
			<x-grid
				style='grid-template-columns: auto 1fr; gap: var(--nm); align-items: start;'>
				<div
					style='background: var(--surface); padding: var(--xs) var(--nm); border-radius: var(--radius-sm); font-weight: bold; white-space: nowrap; font-size: var(--text-sm);'>
					2025-04-10
				</div>
				<x-grid style='gap: var(--xs);'>
					<h3 style='font-size: var(--text-lg); font-weight: bold;'>
						Java SDK 0.9.0 released
					</h3>
					<ul
						style='list-style: disc; padding-left: 1rem; font-size: var(--text-base);'>
						<li>
							Version <strong>0.9.0</strong> of the MCP Java SDK has been released.
						</li>
						<li>Refactored logging system to use exchange mechanism</li>
						<li>Custom Context Paths</li>
						<li>Server Instructions</li>
						<li>CallToolResult Enhancement</li>
					</ul>
				</x-grid>
			</x-grid>

			<!-- Another Entry -->
			<x-grid
				style='grid-template-columns: auto 1fr; gap: var(--nm); align-items: start;'>
				<div
					style='background: var(--surface); padding: var(--xs) var(--nm); border-radius: var(--radius-sm); font-weight: bold; white-space: nowrap; font-size: var(--text-sm);'>
					2025-03-26
				</div>
				<x-grid style='gap: var(--xs);'>
					<h3 style='font-size: var(--text-lg); font-weight: bold;'>
						Kotlin SDK 0.4.0 released
					</h3>
					<ul
						style='list-style: disc; padding-left: 1rem; font-size: var(--text-base);'>
						<li>Fix issues and cleanup API</li>
						<li>
							Added binary compatibility tracking to avoid breaking changes
						</li>
						<li>Drop JDK requirements to JDK8</li>
						<li>Added Claude Desktop integration with sample</li>
						<li>
							See changelog:
							<a
								href='https://github.com/modelcontextprotocol/kotlin-sdk/releases/tag/0.4.0'
								target='_blank'
								style='color: var(--accent); text-decoration: underline;'>
								GitHub Release
							</a>
						</li>
					</ul>
				</x-grid>
			</x-grid>

			<!-- Repeat more entries... -->
		</x-grid>
	</x-grid>
</Layout>
