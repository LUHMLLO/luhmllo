---
layout: '@layouts/documentation.astro'
---

<header>
  ## Input

`<input />` elements are the core of form interactivity. They enable users to provide structured data and perform actions across a wide range of use cases. Each input type is optimized for specific formats, aiding both UX and validation at the browser level.

</header>

<div data-is="grid" style="--cols: 2; --gap: var(--nm);">
  <div data-is="group" style="gap: var(--sm);">
    Standard single-line text input for general text content.

    <div data-is="codeblock">
      <div data-part="example">
        <input type="text" placeholder="Enter text" />
      </div>

      ```html
      <input type="text" placeholder="Enter text" />
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    Obscures input for passwords.

    <div data-is="codeblock">
      <div data-part="example">
        <input type="password" placeholder="Enter password" />
      </div>

      ```html
      <input type="password" placeholder="Enter password" />
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    Email input validates input pattern natively.

    <div data-is="codeblock">
      <div data-part="example">
        <input type="email" placeholder="name@example.com" />
      </div>

      ```html
      <input type="email" placeholder="name@example.com" />
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    Numbers only. Accepts optional min, max, and step attributes.

    <div data-is="codeblock">
      <div data-part="example">
        <input type="number" min="0" max="100" step="1" />
      </div>

      ```html
      <input type="number" min="0" max="100" step="1" />
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    Input field for phone numbers. Browser may present a numeric keyboard on mobile.

    <div data-is="codeblock">
      <div data-part="example">
        <input type="tel" placeholder="(555) 555-5555" />
      </div>

      ```html
      <input type="tel" placeholder="(555) 555-5555" />
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    Accepts URLs and triggers browser-level validation.

    <div data-is="codeblock">
      <div data-part="example">
        <input type="url" placeholder="https://example.com" />
      </div>

      ```html
      <input type="url" placeholder="https://example.com" />
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    File upload input. Accepts files based on type or extension.

    <div data-is="codeblock">
      <div data-part="example">
        <input type="file" accept="image/*" />
      </div>

      ```html
      <input type="file" accept="image/*" />
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    Checkbox input for boolean values.

    <div data-is="codeblock">
      <div data-part="example">
        <label for="check">
          <input type="checkbox" id="check" />
          <span>Accept</span>
        </label>
      </div>

      ```html
      <label for="check">
        <input type="checkbox" id="check" />
        <span>Accept</span>
      </label>
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    Radio buttons for single-choice options within a group.

    <div data-is="codeblock">
      <div data-part="example">
        <label for="one">
          <input type="radio" name="choice" id="one" />
          <span>One</span>
        </label>

        <label for="two">
          <input type="radio" name="choice" id="two" />
          <span>Two</span>
        </label>
      </div>

      ```html
      <label for="one">
        <input type="radio" name="choice" id="one" />
        <span>One</span>
      </label>

      <label for="two">
        <input type="radio" name="choice" id="two" />
        <span>Two</span>
      </label>
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    Date input. Enables native date picker UI in most browsers.

    <div data-is="codeblock">
      <div data-part="example">
        <input type="date" />
      </div>

      ```html
      <input type="date" />
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    Range slider for selecting a numeric value within a defined span.

    <div data-is="codeblock">
      <div data-part="example">
        <input type="range" min="0" max="100" value="50" />
      </div>

      ```html
      <input type="range" min="0" max="100" value="50" />
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    Color picker input for choosing colors with native browser UI.

    <div data-is="codeblock">
      <div data-part="example">
        <input type="color" value="#ff0000" />
      </div>

      ```html
      <input type="color" value="#ff0000" />
      ```
    </div>

  </div>

  <div data-is="group" style="gap: var(--sm);">
    Search input optimized for search actions, often includes browser-native enhancements.

    <div data-is="codeblock">
      <div data-part="example">
        <input type="search" placeholder="Search..." />
      </div>

      ```html
      <input type="search" placeholder="Search..." />
      ```
    </div>

  </div>
</div>
