---
import ShellLayout from '@layouts/app.astro';
import {
	getTopLevelRoutesByGroup,
	type RouteItem,
} from 'src/utils/getPageGroup';

const pages: RouteItem[] = getTopLevelRoutesByGroup('projects').filter(
	(page) => page.name !== 'projects'
);
---

<ShellLayout>
	<section>
		<h2>Projects</h2>

		<br />

		<div data-is='grid' style='--gap: var(--md); --cols: 4;'>
			{
				pages.map((el) => (
					<a href={el.path}>
						<span>{el.name}</span>
						<i data-is='icon'>north_east</i>
					</a>
				))
			}
		</div>
	</section>
</ShellLayout>
