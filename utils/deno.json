{
  "lock": false,
  "nodeModulesDir": "auto",
  "name": "@luhmllo/utils",
  "exports": {
    ".": "./dist"
  },
  "tasks": {
    "check": "deno fmt --check && deno lint && deno check **/*.ts && deno check **/*.tsx",
    "build": "deno run -A ./build.ts && cp dist/*.js ../docs/static/vendors/"
  }
}
